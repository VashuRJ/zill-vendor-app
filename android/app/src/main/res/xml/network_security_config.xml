<?xml version="1.0" encoding="utf-8"?>
<!-- Zill Restaurant Partner: enforce HTTPS-only, system CA trust only. -->
<network-security-config>

    <!-- Block all cleartext (http) traffic. System CA store only in release. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug builds only: also trust user-installed CAs (Burp/Charles). -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

</network-security-config>
